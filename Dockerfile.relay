FROM oven/bun:latest

WORKDIR /app

COPY packages/relay/package.json packages/relay/bun.lock ./
RUN bun install --production

COPY packages/relay/src ./src
COPY packages/relay/tsconfig.json ./

CMD ["bun", "run", "src/index.ts"]
